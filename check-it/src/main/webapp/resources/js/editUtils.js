(function($){

	/*
	$.extend({
		editorWrapper: function (id) {
			var editor = Jodit.make(id, {
				"uploader": {
					"insertImageAsBase64URI": true
				}
			});
			return editor;
		},
	});
	*/

})(jQuery);
